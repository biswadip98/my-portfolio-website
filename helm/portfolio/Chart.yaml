apiVersion: v2
name: portfolio
description: Helm chart for Biswadip's three-tier DevOps portfolio (frontend, backend, Postgres)
type: application

# Chart version: the packaging version. Bump when the chart changes.
version: 0.1.0

# appVersion: the version of the application (matches our container image tag).
appVersion: "1.0.0"
